local _ = require("gettext")

return {
    name = "gdrive",
    fullname = _("Google Drive"),
    description = _([[Two-way sync highlights, reading progress and vocabulary across devices via Google Drive.]]),
}
